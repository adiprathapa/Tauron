[pytest]
# Run: venv/bin/python -m pytest          (all tests)
#      venv/bin/python -m pytest backend/tests/test_endpoints.py -v
#      venv/bin/python -m pytest backend/tests/test_ingest_csv.py -v  (requires PyTorch)
#
# Always use the project venv — system Python lacks fastapi/torch.

testpaths     = backend/tests
python_files  = test_*.py
python_classes = Test*
python_functions = test_*
addopts       = -v
